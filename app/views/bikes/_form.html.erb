<%= form_with(model: bike) do |form| %>
  <% if bike.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bike.errors.count, "error") %> prohibited this bike from being saved:</h2>

      <ul>
        <% bike.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :brand, style: "display: block" %>
    <%= form.text_field :brand %>
  </div>

  <div>
    <%= form.label :wheels, style: "display: block" %>
    <%= form.number_field :wheels %>
  </div>

  <div>
    <%= form.label :usage, style: "display: block" %>
    <%= form.text_field :usage %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
